{
  "name": "9-or-10-mafia",
  "version": "1.0.0",
  "description": "",
  "dependencies": {
    "@aws-sdk/client-s3": "^3.1022.0",
    "@aws-sdk/client-ses": "^3.1023.0",
    "bcryptjs": "^3.0.2",
    "cors": "^2.8.5",
    "express": "^4.19.2",
    "jsonwebtoken": "^9.0.2",
    "mongodb": "^6.13.0",
    "serverless-http": "^3.2.0"
  },
  "scripts": {
    "start": "npx serverless offline",
    "check-deploy": "aws cloudformation describe-stacks --stack-name mafia9or10-dev --region us-west-2 --query \"Stacks[0].StackStatus\" --profile mafia9or10",
    "deploy": "export AWS_PROFILE=mafia9or10 && serverless deploy",
    "remove": "export AWS_PROFILE=mafia9or10 && serverless remove"
  },
  "devDependencies": {
    "serverless-dotenv-plugin": "^6.0.0",
    "serverless-offline": "^14.4.0"
  }
}
